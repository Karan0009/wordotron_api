services:
  api:
    build:
      target: dev
    volumes:
      - .:/app
      - go-mod-cache:/go/pkg/mod
      - go-build-cache:/root/.cache/go-build
      - uploads-data:/app/.data/uploads

volumes:
  go-mod-cache:
  go-build-cache:
